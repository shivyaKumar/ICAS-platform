'use client'; // keep this if you use hooks/state on this page

import React from 'react'; // optional, but fine

export default function StaffDashboardPage() {
  return (
    <main className="p-6">
      <h1>Staff Dashboard</h1>
    </main>
  );
}

// Optional: ensures TS treats the file as a module even if you remove imports later
export {};
